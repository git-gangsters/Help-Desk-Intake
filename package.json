{
  "name": "help-desk-intake",
  "version": "1.0.0",
  "private": true,
  "description": "Lightweight, browser-based help desk intake and ticket tracker (static HTML/CSS/JS).",
  "scripts": {
    "lint": "npm run lint:html && npm run lint:css && npm run lint:js",
    "lint:html": "htmlhint \"./*.html\"",
    "lint:css": "stylelint \"./*.css\"",
    "lint:js": "eslint --max-warnings=0 \"./*.js\""
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.0.0",
    "globals": "^17.3.0",
    "htmlhint": "^1.8.1",
    "stylelint": "^17.1.1",
    "stylelint-config-standard": "^40.0.0"
  }
}
