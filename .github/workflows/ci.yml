name: CI

on: [push, pull_request]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Static project – no build required"

- name: Claude Code Action Official
- uses: anthropics/claude-code-action@v1
